# ===== ⚠️ 这是生产凭证模板 =====
# 填值后复制为 .env（生产环境用，docker-compose.prod.yml 会加载）
# 本地开发请用 .env.local.example → .env.local，本地凭证优先级更高
# 加载顺序：os.environ > .env.local > 根 .env（pydantic-settings 后者覆盖前者）
# 完整说明：docs/operations/local-dev.md §2.2（必须建 .env.local 避免污染生产）

# ===== Langfuse Cloud（trace） =====
LANGFUSE_BASE_URL=https://cloud.langfuse.com
LANGFUSE_PUBLIC_KEY=pk-lf-xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
LANGFUSE_SECRET_KEY=sk-lf-xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
# 前端用的 Langfuse Project ID（用于生成 trace 跳转链接）
NEXT_PUBLIC_LANGFUSE_PROJECT_ID=cmqnxbcncxxxxxxxxxxxxxxxxxx

# ===== MySQL（由 miao-infra 提供，复用 miao-mysql 容器） =====
# host 写 miao-mysql（miao-infra-net 网络内的服务名），不是 localhost
DATABASE_URL=mysql+aiomysql://miao:REPLACE_WITH_PASSWORD@miao-mysql:3306/miao_ai?charset=utf8mb4

# ===== 腾讯云 COS（对象存储） =====
# 详见 infra/CLOUD_SETUP.md
TENCENT_SECRET_ID=...
TENCENT_SECRET_KEY=...
TENCENT_REGION=ap-shanghai
TENCENT_BUCKET=miao-agents-<APPID>
COS_ENDPOINT=https://cos.ap-shanghai.myqcloud.com

# ===== DashScope（LLM provider，OpenAI 兼容） =====
DASHSCOPE_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
DASHSCOPE_MODEL=qwen-plus

# ===== Secret Encryption（Fernet，用于加密 Provider API Key） =====
# 生成方式：python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=
