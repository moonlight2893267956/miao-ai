# Miao AI — 项目约定

## 文档同步规则

**架构文档同步更新**: 任何涉及架构调整的代码变更，必须同步更新 `docs/ARCHITECTURE.md`。包括但不限于：

- 新增/删除 API 端点
- DB 模型 schema 变更（新增字段、改列类型、增删表）
- 运行时架构变化（新增模块、改变进程生命周期、新增运行时模式）
- 配置项新增/删除/默认值变更
- 前端页面路由或核心组件调整
- 外部服务依赖变更（新增/替换云服务）

不涉及架构的改动（纯 bug fix、样式调整、日志优化等）无需更新。
